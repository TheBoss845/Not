import React,{useEffect,useMemo,useState} from 'react'; import {Check,Play,Plus,ThumbsUp,X} from 'lucide-react'; import {gradientFor} from '../data/catalog.js'; import {useNotflix} from '../context/NotflixContext.jsx'; import FocusButton from './FocusButton.jsx';
export default function TitleModal({item,onClose,onPlay,onOpenTitle}){const{myList,toggleMyList,catalog}=useNotflix();const[seasonNumber,setSeasonNumber]=useState(item?.seasons?.[0]?.number??1);const[liked,setLiked]=useState(false);useEffect(()=>setSeasonNumber(item?.seasons?.[0]?.number??1),[item?.id]);const season=item?.seasons?.find((s)=>s.number===seasonNumber);const related=useMemo(()=>!item?[]:catalog.filter((candidate)=>candidate.id!==item.id&&candidate.genres.some((g)=>item.genres.includes(g))).slice(0,6),[item,catalog]);if(!item)return null;const inList=myList.includes(item.id);return <div className="modal-backdrop" onMouseDown={(e)=>e.target===e.currentTarget&&onClose()}><section className="title-modal" role="dialog" aria-modal="true"><div className="title-modal__hero" style={{background:gradientFor(item.tone)}}><div className="title-modal__art">{item.emoji}</div><div className="title-modal__fade"/><FocusButton className="close-button" onClick={onClose}><X size={22}/></FocusButton><div className="title-modal__hero-content"><div className="eyebrow">{item.kicker}</div><h2>{item.title}</h2><div className="title-modal__actions"><FocusButton className="hero-button hero-button--play" onClick={()=>onPlay(item)}><Play size={21} fill="currentColor"/> Play</FocusButton><FocusButton className="round-button round-button--large" onClick={()=>toggleMyList(item.id)}>{inList?<Check/>:<Plus/>}</FocusButton><FocusButton className={`round-button round-button--large ${liked?'round-button--liked':''}`} onClick={()=>setLiked((v)=>!v)}><ThumbsUp/></FocusButton></div></div></div><div className="title-modal__body"><div className="title-modal__primary"><div className="detail-meta"><strong>{item.match}% Match</strong><span>{item.year}</span><span className="rating-pill">{item.rating}</span><span>{item.runtime}</span><span className="hd-pill">HD</span></div><p className="detail-description">{item.description}</p></div><aside className="title-modal__aside"><p><span>Cast:</span> {item.cast.join(', ')}</p><p><span>Genres:</span> {item.genres.join(', ')}</p><p><span>This show is:</span> Absurd, Suspiciously Polished, Questionable</p></aside>{item.type==='show'&&<section className="episodes-section"><div className="episodes-header"><h3>Episodes</h3><label className="season-select-wrap"><select value={seasonNumber} onChange={(e)=>setSeasonNumber(Number(e.target.value))}>{item.seasons.map((s)=><option key={s.number} value={s.number}>Season {s.number}</option>)}</select></label></div><div className="episode-list">{season?.episodes.map((ep)=><FocusButton className="episode-row" key={ep.id} onClick={()=>onPlay(item,{...ep,seasonNumber})}><span className="episode-row__number">{ep.number}</span><span className="episode-row__thumb" style={{background:gradientFor(ep.tone??item.tone)}}><span>{ep.emoji??item.emoji}</span><Play size={22} fill="white"/></span><span className="episode-row__copy"><strong>{ep.title}</strong><span>{ep.description}</span></span><span className="episode-row__duration">{ep.duration}</span></FocusButton>)}</div></section>}<section className="more-like-this"><h3>More Like This</h3><div className="more-grid">{related.map((relatedItem)=><FocusButton key={relatedItem.id} className="more-card" onClick={()=>onOpenTitle(relatedItem)}><span className="more-card__art" style={{background:gradientFor(relatedItem.tone)}}><span>{relatedItem.emoji}</span><strong>{relatedItem.title}</strong></span><span className="more-card__copy"><span><b>{relatedItem.match}% Match</b> · {relatedItem.rating}</span><p>{relatedItem.description}</p></span></FocusButton>)}</div></section><section className="about-title"><h3>About {item.title}</h3><p><span>Creators:</span> {item.creators.join(', ')}</p><p><span>Genres:</span> {item.genres.join(', ')}</p><p><span>Maturity rating:</span> {item.rating} · Contains scenes of irresponsible decision-making.</p></section></div></section></div>;}

import React,{useRef,useState} from 'react'; import {ChevronDown,Check,Plus,Play} from 'lucide-react'; import {gradientFor} from '../data/catalog.js'; import {useNotflix} from '../context/NotflixContext.jsx'; import FocusButton from './FocusButton.jsx';
export default function MediaCard({item,rank,progressValue=0,onInfo,onPlay}){const{myList,toggleMyList}=useNotflix();const[expanded,setExpanded]=useState(false);const timer=useRef(null);const inList=myList.includes(item.id);const startExpand=()=>{clearTimeout(timer.current);timer.current=setTimeout(()=>setExpanded(true),450);};const stopExpand=()=>{clearTimeout(timer.current);timer.current=setTimeout(()=>setExpanded(false),120);};return <article className={`media-card-wrap ${rank?'media-card-wrap--ranked':''}`}>{rank&&<div className="rank-number">{rank}</div>}<div className={`media-card ${expanded?'media-card--expanded':''}`} onMouseEnter={startExpand} onMouseLeave={stopExpand} onFocusCapture={startExpand} onBlurCapture={stopExpand}><FocusButton className="media-card__art" onClick={()=>onInfo(item)}><span className="media-card__gradient" style={{background:gradientFor(item.tone)}}/><span className="media-card__emoji">{item.emoji}</span><span className="media-card__title">{item.title}</span>{item.newRelease&&<span className="media-card__new">NEW</span>}{progressValue>0&&<span className="progress-track"><span style={{width:`${progressValue*100}%`}}/></span>}</FocusButton><div className="media-card__details"><div className="media-card__buttons"><FocusButton className="round-button round-button--play" onClick={()=>onPlay(item)}><Play size={16} fill="currentColor"/></FocusButton><FocusButton className="round-button" onClick={()=>toggleMyList(item.id)}>{inList?<Check size={17}/>:<Plus size={17}/>}</FocusButton><FocusButton className="round-button round-button--right" onClick={()=>onInfo(item)}><ChevronDown size={18}/></FocusButton></div><div className="media-card__meta"><strong>{item.match}% Match</strong><span className="rating-mini">{item.rating}</span><span>{item.runtime}</span></div><div className="media-card__genres">{item.genres.slice(0,3).map((g,i)=><React.Fragment key={g}><span>{g}</span>{i<Math.min(item.genres.length,3)-1&&<b>•</b>}</React.Fragment>)}</div></div></div></article>;}

import React,{useEffect,useState} from 'react';
import {Info,Play,Volume2,VolumeX} from 'lucide-react'; import {gradientFor} from '../data/catalog.js'; import FocusButton from './FocusButton.jsx';
export default function Hero({item,onPlay,onInfo}){const[muted,setMuted]=useState(true);const[previewing,setPreviewing]=useState(false);useEffect(()=>{const id=setTimeout(()=>setPreviewing(true),1900);return()=>clearTimeout(id);},[item?.id]);if(!item)return null;return <section className={`hero ${previewing?'hero--previewing':''}`} style={{'--hero-bg':gradientFor(item.tone)}}><div className="hero__visual"><div className="hero__orb hero__orb--a"/><div className="hero__orb hero__orb--b"/><div className="hero__emoji">{item.emoji}</div><div className="hero__grain"/></div><div className="hero__vignette"/><div className="hero__content"><div className="eyebrow">{item.kicker}</div><h1>{item.title}</h1><p className="hero__tagline">{item.tagline}</p><p className="hero__description">{item.description}</p><div className="hero__meta"><strong>{item.match}% Match</strong><span>{item.year}</span><span className="rating-pill">{item.rating}</span><span>{item.runtime}</span></div><div className="hero__actions"><FocusButton className="hero-button hero-button--play" onClick={()=>onPlay(item)}><Play fill="currentColor" size={24}/> Play</FocusButton><FocusButton className="hero-button hero-button--info" onClick={()=>onInfo(item)}><Info size={24}/> More Info</FocusButton></div></div><div className="hero__controls"><FocusButton className="hero-volume" onClick={()=>setMuted((v)=>!v)}>{muted?<VolumeX size={20}/>:<Volume2 size={20}/>}</FocusButton><span className="hero__rating">{item.rating}</span></div>{previewing&&<div className="hero__preview-label">PREVIEW</div>}</section>;}

import React from 'react'; import {ChevronLeft,LogOut,RotateCcw,X} from 'lucide-react'; import {useNotflix} from '../context/NotflixContext.jsx'; import FocusButton from './FocusButton.jsx';
function Toggle({checked,onChange,label,description}){return <div className="setting-row"><div><strong>{label}</strong>{description&&<span>{description}</span>}</div><FocusButton className={`toggle ${checked?'toggle--on':''}`} onClick={()=>onChange(!checked)} aria-pressed={checked}><span/></FocusButton></div>;}
export default function SettingsPanel({onClose}){const{profile,settings,setSettings,signOutProfile,clearNotifications,notify}=useNotflix();const patch=(key,value)=>setSettings((prev)=>({...prev,[key]:value}));return <div className="settings-backdrop"><aside className="settings-panel"><div className="settings-panel__header"><div><small>Profile</small><h2>{profile?.avatar} {profile?.name}</h2></div><FocusButton className="close-button close-button--static" onClick={onClose}><X/></FocusButton></div><section><h3>Playback</h3><Toggle checked={settings.autoplayPreviews} onChange={(v)=>patch('autoplayPreviews',v)} label="Autoplay previews" description="Start previews while browsing."/><Toggle checked={settings.autoplayNext} onChange={(v)=>patch('autoplayNext',v)} label="Autoplay next episode" description="Continue the binge automatically."/><Toggle checked={settings.subtitles} onChange={(v)=>patch('subtitles',v)} label="Subtitles" description="Show subtitles by default."/><div className="setting-row"><div><strong>Subtitle size</strong><span>Choose how aggressively the captions occupy the television.</span></div><select value={settings.subtitleSize} onChange={(e)=>patch('subtitleSize',e.target.value)}><option value="small">Small</option><option value="medium">Medium</option><option value="large">Large</option></select></div></section><section><h3>Experience</h3><Toggle checked={settings.reduceMotion} onChange={(v)=>patch('reduceMotion',v)} label="Reduce motion" description="Calm down transitions and previews."/><Toggle checked={settings.soundEffects} onChange={(v)=>patch('soundEffects',v)} label="Interface sounds" description="Future-ready switch for TV navigation sounds."/></section><section><h3>Account-ish Things</h3><FocusButton className="settings-action" onClick={()=>{clearNotifications();notify('Notifications cleared');}}><RotateCcw size={18}/> Clear notifications</FocusButton><FocusButton className="settings-action" onClick={()=>{signOutProfile();onClose();}}><LogOut size={18}/> Switch profile</FocusButton></section><footer><ChevronLeft size={14}/> TV remote: use arrow keys, Enter and Back/Escape.</footer></aside></div>;}

import React,{useMemo} from 'react'; import {baseRows,navFilters} from '../data/rows.js'; import {useNotflix} from '../context/NotflixContext.jsx'; import Hero from '../components/Hero.jsx'; import MediaRow from '../components/MediaRow.jsx'; import MediaCard from '../components/MediaCard.jsx';
export default function BrowsePage({page,onInfo,onPlay}){const{catalog,myList,progress,injected}=useNotflix();const hero=useMemo(()=>{if(injected.length)return catalog.find((item)=>item.id===injected[0])??catalog[0];if(page==='TV Shows')return catalog.find((item)=>item.type==='show')??catalog[0];if(page==='Movies')return catalog.find((item)=>item.type==='movie'&&item.featured)??catalog.find((item)=>item.type==='movie');return catalog.find((item)=>item.featured)??catalog[0];},[catalog,page,injected]);if(page==='My List'){const items=myList.map((id)=>catalog.find((x)=>x.id===id)).filter(Boolean);return <main className="library-page page-shell"><div className="library-page__heading"><span className="eyebrow">YOUR LIBRARY</span><h1>My List</h1><p>{items.length} title{items.length===1?'':'s'} saved for later, assuming later ever actually arrives.</p></div>{items.length?<div className="library-grid">{items.map((item)=><MediaCard key={item.id} item={item} progressValue={progress[item.id]??0} onInfo={onInfo} onPlay={onPlay}/>)}</div>:<div className="empty-state"><h2>Your list is empty</h2><p>Add a few terrible cinematic decisions from the home screen.</p></div>}</main>;}if(page!=='Home'){const predicate=navFilters[page]??(()=>true);const items=catalog.filter(predicate);return <main className="genre-page page-shell"><section className="compact-hero"><div className="eyebrow">NOTFLIX COLLECTION</div><h1>{page}</h1><p>{page==='TV Shows'?'Series built for “one more episode” becoming an administrative problem.':page==='Movies'?'Original parody movies, fake blockbusters, and terrible decisions in widescreen.':'New releases, trending disasters, and the titles the algorithm has decided everyone must see.'}</p></section><div className="library-grid">{items.map((item)=><MediaCard key={item.id} item={item} progressValue={progress[item.id]??0} onInfo={onInfo} onPlay={onPlay}/>)}</div></main>;}return <main className="browse-page"><Hero item={hero} onPlay={onPlay} onInfo={onInfo}/><div className="rows-stack">{baseRows.map((row)=><MediaRow key={row.id} row={row} onInfo={onInfo} onPlay={onPlay}/>)}</div><footer className="site-footer"><strong>NOTFLIX</strong><span>Parody streaming prototype · Original or properly licensed media only.</span><span>Built to run on a browser, TV, and eventually a tiny computer hidden somewhere sensible and permitted.</span></footer></main>;}

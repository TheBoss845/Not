# Prank engine

Current reusable effects include Premium Pause, Premium Rewind, fake buffering, subtitle notices, fake service errors, fake HD upsells, and the final NOTFLIX reveal.

A title can opt into pranks with manifest data such as `pause`, `rewind`, or `atPercent` triggers. HQ can trigger the same effects remotely.

These are visual jokes only. They should never collect payment details, credentials, or imitate a real account transaction.
